'use strict';

/**
 * @ngdoc function
 * @name psmsApp.controller:dashboardCtrl
 * @description
 * # dashboardCtrl
 * Controller of the psmsApp
 */ 

var app = angular.module('psmsApp');
app.controller('dashboardCtrl',['$scope', '$rootScope', '$cookies', '$window', '$location', '$timeout', 'swalert',
  function ($scope, $rootScope, $cookies, $window, $location, $timeout, swalert) {

  var dc = this;



}]);
